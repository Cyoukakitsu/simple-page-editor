# CONTEXT

フロントエンドプロジェクト: NCDC フロントエンド採用課題（recruit-frontend）。対象の後端リポジトリは `ncdcdev/recruit-frontend`（NestJS + TypeORM/SQLite）。

## 用語集（Glossary）

- **Page（ページ）**: 本プロジェクトの中心となる業務オブジェクトで、UI・フロントエンドコードにおける正式名称。コンポーネント・hooks・ルートのドメイン命名には一律 `Page` を使う（`PageEditor`、各コンポーネント内の `Page` 型など。一覧表示は `Sidebar` が担当する ── こちらは下記に別途定義した用語で、画面上の位置に由来する名前）。後端エンティティ `Content`（フィールド: `id`, `title`, `body`, `createdAt`, `updatedAt`）に対応する。**`Content` という名前は API データ層（`src/api/` など）の中だけに閉じ込める**こと。UI 層のコンポーネントや業務ロジックの命名に漏らさない。
- **Untitled Page（無題）**: `title` が空文字の Page。サイドバーではプレースホルダー文言「無題」を表示し、通常のタイトルとは見た目を変える（グレー＋斜体）。通常どおり編集・保存できる。その行が**選択中のときは選択状態の文字色を優先し、無題であることは斜体だけで示す**（文字色を2つ重ねると、どちらが勝つかがスタイルの出力順任せになってしまうため、色は必ずどちらか一方に決める）。
- **Sidebar（サイドバー）**: 全 Page の一覧を表示する。データソースは `GET /content` そのもの（「サイドバー専用」のエンドポイントは存在しない）。並び順は `createdAt` の**降順**（最後に作成したものが一番上）とし、後端が返す順序に依存せずフロント側でソートする。
- **List Edit Mode（一覧編集モード）**: サイドバー下部の「Edit」ボタンで入るモード。各 Page 行に削除（ゴミ箱）アイコンが現れ、下部のボタンが「New page」／「Done」に変わる。これはデザイン仕様が定義した操作であり、要件文書にある文字どおりの「－」ボタンではないが、両者は同じ機能（ページの削除）を指す。
- **Title Edit / Body Edit（タイトル編集／本文編集）**: Page 詳細エリアのタイトルと本文はそれぞれ独立した編集状態（各々の Edit → Cancel/Save）を持ち、各々独立した `PUT /content/:id` の呼び出しに対応する（DTO の `title` / `body` はどちらも任意フィールドなので、片方だけを更新できる）。

**タイトル／本文の文字数制約**（1〜50 / 10〜2000 文字）は後端が定義済みの業務ルールであり、フロント側で新たに決めた概念ではない。そのためここでは「決定事項」として重複して並べない。具体的な数値は後端の DTO / entity を正とする。
