# QUALITY_SCORE.md

提出前の自己採点シート。後端リポジトリの `doc/frontend-challenge.md` に書かれた**評価の観点**をそのまま採点項目にしている（勝手に基準を作らない）。

## 使い方

1. 「QUALITY_SCORE.md で採点して」と頼む
2. 採点するのは、**実装したセッションとは別の、文脈を持たないエージェント**にする。実装者は自分のコードを甘く評価するため（自己評価バイアス）。採点役には「懐疑的な評価者」として振る舞わせる
3. エージェントは各項目に **スコア（1〜5）** と **根拠**（該当ファイル・行、またはコマンドの実行結果）と **改善案** を記入し、下の採点履歴に追記する
4. 指摘に対応したら、もう一度採点を依頼する。スコアの推移を履歴に残す

### 採点のルール

- **根拠を書いてからスコアを書く**（逆順にしない。先に点数を決めると根拠が後付けになる）
- **文書の記述は根拠にしない**。README や CLAUDE.md が「実装した」と書いていても、該当するコードを見つけて初めて加点する
- **実際に動かして確かめる**（`pnpm run typecheck` / `test` / `lint`、バックエンドを起動してブラウザで操作）。コードを読んだだけの推測は「未確認」と書く
- 確認できないものは「未確認」と書き、5点にしない
- CLAUDE.md / CONTEXT.md に**意図的な決定として記録されている事項**（状態管理ライブラリ不採用、`src/features/*` にテストを置かない等）は、「不足」ではなく決定の妥当性として評価する

### スコアの目安（点数のインフレを防ぐための基準）

| 点 | 意味 |
|---|---|
| 5 | 業務コードとしてそのままレビューを通せる。意図が明示され、例外ケースも処理されている |
| 4 | 良好。指摘は好みの範囲、または影響の小さいもの |
| 3 | 要件は満たすが、レビューで修正を求められる箇所がある |
| 2 | 動くが設計・可読性に明確な問題がある |
| 1 | 要件を満たしていない、または動かない |

## 採点項目（後端リポジトリの評価基準に対応）

| # | 項目 | 内容 |
|---|---|---|
| 1 | 設計思想・意図 | README に設計判断とその理由が書かれているか（設計書は不要、README で十分） |
| 2 | 共通化・コンポーネント化 | 重複コードがないか、コンポーネント/hooksの粒度が適切か |
| 3 | 非レガシー | 古い書き方（クラスコンポーネント、any の濫用、非推奨API等）をしていないか |
| 4 | 可読性 | 命名、関数の長さ、コメントの付け方（README/[CONTEXT.md](./CONTEXT.md) 参照） |
| 5 | 機能・デザイン仕様の充足 | `frontend-challenge.md` の要件4項目 + バリデーション制約を満たしているか、デザイン仕様（色・フォント等）を再現しているか |
| 6 | レスポンシブ対応（加点） | 小画面でも壊れずに使えるか |

### 別枠（合否ゲート。スコアではなく Pass/Fail）

- テストコードが1つ以上ある
- タイトル: 1〜50文字、本文: 10〜2000文字のバリデーションが実装されている
- `pnpm run typecheck` / `pnpm run test` / `pnpm run lint` が通る

## 採点履歴

<!-- 採点するたびに下に追記していく。古い記録は消さない -->

### 2026-09-17 第1回（文脈を持たない採点エージェント2体 + 実装者による突き合わせ）

採点役: 実装セッションとは別の、懐疑的な評価者エージェント（課題要件の原本 → コード → 実ブラウザ操作の順に確認）。
併走: 過度な設計だけを探すレビューエージェント。実装者は両者の指摘の裏取り（行番号・再現性の照合）のみ担当。

| # | 項目 | スコア | 根拠（要約） |
|---|---|---|---|
| 1 | 設計思想・意図 | 4/5 | README に技術選定の理由と不採用の判断（状態管理・ルーティング）が理由付きで書かれている。README の「追加実装」10項目はすべて対応コードを特定でき、記述と実装の食い違いは 0 件 |
| 2 | 共通化・コンポーネント化 | 3/5 | `src/api/pageApi.ts` の `request()`、`Button`、`usePages` への集約は適切。一方 `TitleSection` と `BodySection` が構造上ほぼ双子（state・保存の try/catch・Cancel/Save ブロックの約25行が重複） |
| 3 | 非レガシー | 5/5 | 関数コンポーネント + hooks のみ、`any` 0 件、非推奨 API 無し。typecheck / lint とも無警告 |
| 4 | 可読性 | 4/5 | 命名は CONTEXT.md の用語に一致（`Content` は `src/api/` に閉じ込め）。コメントは「なぜ」を書けている。減点は上記の双子構造の読み分けコスト |
| 5 | 機能・デザイン仕様の充足 | 3/5 | 要件4項目は実動作で確認済み。デザインは Sketch の rect と実測が一致（サイドバー280 / 行44 / ボタン90x40・40x40 / 色板6色）。**減点理由は下記の欠陥2件** |
| 6 | レスポンシブ対応（加点） | 4/5 | 390x760 で単ペイン切替・横スクロール無しを実機確認。長いタイトル時の行崩れ（欠陥2）は狭い画面でも同様に出る |

合否ゲート: 3つとも Pass（typecheck / lint 無警告、テスト 12 passed）。ただしバリデーションは**欠陥付きの Pass**（下記1）。

#### 検出された欠陥（実動作で再現確認済み）

1. **バリデーションを UI から破れる** — `TitleSection.tsx:41` / `BodySection.tsx:40` は `trim()` 後の長さで判定するが、保存は `onSave(draft)` と trim 前の値を送る（`TitleSection.tsx:67` / `BodySection.tsx:73`）。前後に空白を付けると 52 文字のタイトル・2002 文字の本文が実際に保存できる。課題必須の制約を満たしていない
2. **長いタイトルで一覧の行が崩れる** — `Sidebar.tsx:56` の行は `h-11` 固定なのに、ラベル（`:63-71`）に `truncate` / `min-w-0` が無い。50文字（仕様上許される長さ）で行が溢れて隣と重なる

#### 過度な設計レビュー（削減提案のうち、実装者が妥当と判断したもの）

- 未使用依存 `@tanstack/react-query-devtools`（`src/` から import 0 件）
- `@types/node` と `tsconfig.node.json` の `"types": ["node"]`（`vite.config.ts` は node API を使っていない）
- 未使用のデザイントークン `--color-bg-canvas-dark`（`src/index.css:12`）
- `Button.test.tsx` だけ `vitest` を明示 import（`globals: true` なので `pageApi.test.ts` と揃っていない）

却下した指摘: コメントの `---- 見出し ----` 規約（このリポジトリの決定事項）、`src/lib/queryClient.ts` の `main.tsx` へのインライン化（ディレクトリ方針を崩す）、クラス結合イディオムの統一（好みの範囲）。

#### 指摘への対応（同日）

| 指摘 | 対応 | 確認方法 |
|---|---|---|
| 欠陥1: 判定は trim 後・保存は trim 前で、上限を超えた値が保存できる | 判定と保存を `src/features/page-editor/useEditSection.ts` に集約し、`onSave(draft.trim())` に統一 | `useEditSection.test.ts` を追加（trim を外すと落ちることを確認済み）。実バックエンドでも前後に空白を付けたタイトルが trim されて保存されることを確認 |
| 欠陥2: 長いタイトルで一覧の行が崩れる | `Sidebar.tsx` のラベルに `min-w-0 truncate` | 45文字のタイトルで、行の高さが 44px のまま省略記号で収まることをブラウザで確認 |
| 項目2: `TitleSection` / `BodySection` の重複 | 編集状態と保存を `useEditSection`、Cancel/Save と Edit ボタンを `EditActions.tsx` に切り出し。両セクションは表示の違い（input / textarea + カウンタ）だけを持つ | 両セクションとも実ブラウザで編集・保存・キャンセルを確認 |
| 過度な設計 | `@tanstack/react-query-devtools`（未使用）、`@types/node` と `tsconfig.node.json` の `types`、`--color-bg-canvas-dark`、`Button.test.tsx` の重複 import を削除 | `pnpm run build` 成功、テスト 15 passed |

次回の採点時は、この対応後の状態を**別の文脈を持たないエージェント**に再評価させる（実装者が自分で点数を上げない）。

### 2026-09-17 第2回（静的解析ツール react-doctor による検査）

実行: `npx -y react-doctor@0.9.14 -y --verbose`（millionco 製の React 向け静的解析。プロジェクトの依存には入れず npx で1回だけ実行）。18 ファイルを走査。

| 回 | スコア | 検出件数 | 内訳 |
|---|---|---|---|
| 修正前 | 56/100 | 4 | Security 2 / Accessibility 2 |
| 修正後 | 49/100 | 2 | Security 2 |

**スコアの読み方に注意**: 件数が 4→2 に減ったのにスコアは下がった。このツールのスコアは残った問題の比重で決まるらしく、絶対値は品質の指標として当てにならない。**件数と内容で判断する**こと。

#### 対応した指摘

| 指摘 | 内容 | 対応 |
|---|---|---|
| `control-has-associated-label`（Accessibility, 2件） | `TitleSection.tsx` の `<input>` と `BodySection.tsx` の `<textarea>` に読み上げ用の名前が無く、スクリーンリーダー利用者が何の入力欄か分からない。デザイン上、入力欄は表示と入れ替わる形で見えるラベルが存在しないため、実際に名前が無い状態だった | `aria-label="タイトル"` / `aria-label="本文"` を付与。あわせて文字数カウンタに `id="body-counter"` を付け、`aria-describedby` で本文の入力欄と結びつけた（制限の説明が読み上げられる） |

#### 見送った指摘

| 指摘 | 内容 | 検討 |
|---|---|---|
| `require-pnpm-hardening`（Security, 2件） | `pnpm-workspace.yaml` に `minimumReleaseAge`（公開直後の版を一定期間入れない）と `trustPolicy: no-downgrade` が無い | このリポジトリに `pnpm-workspace.yaml` は存在せず（workspace ではない）、新規作成して設定を置くことになる。依存の供給網に関する運用設定であり、課題の評価対象である React の実装品質とは別の層。また評価者が `pnpm install` する際の解決挙動を変える可能性がある。**採用課題の提出物としては見送る**（本番運用のリポジトリなら入れる価値がある） |

なお react-doctor 自体もプロジェクトには入れない（`npx` で1回だけ実行した）。未使用の依存を削ったばかりで、常時必要なツールではないため。
