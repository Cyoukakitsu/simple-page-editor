# INTERACTION

サイドバー・メインエリアの UI インタラクション仕様。`Design/画面/20220615/` の4枚のモックアップ（`01_default.png` / `02_menu_edit.png` / `03_title_edit.png` / `04_text_edit.png`）と `Design/DesignSpec/`、`CONTEXT.md`、後端リポジトリの `doc/frontend-challenge.md` / `src/content/*` を突き合わせて整理したもの。

**このファイルの位置づけ**: 画面の状態遷移とイベントのトリガーを実装前に確定させるための資料。ドメイン用語・命名規則は `CONTEXT.md` を参照。デザインの寸法・色・フォントは `Design/DesignSpec/` を参照（ここには書かない）。

## 画面構成

- **Sidebar**（左、固定幅）: 全 Page のリスト。データソースは `GET /content` のみ（サイドバー専用 API はない）。並び順は `createdAt` 降順、フロント側でソートする。
- **MainArea**（右）: 選択中の Page の詳細。タイトルと本文で構成され、それぞれ独立した編集状態を持つ。

Sidebar のリスト項目クリック＝そのページを選択して MainArea に表示（画面遷移というより選択状態の切り替え）。選択中の項目は行全体をハイライトする（具体的な色・寸法は `01_default.png` の注釈と `Design/DesignSpec/` を参照）。

## レスポンシブ（狭い画面）

デザイン仕様に縮小時の指定が無いため、**768px（Tailwind の `md`）未満では 2 カラムをやめ、単ペインにする**（決定事項）。新しい状態は増やさず、既存の「選択中の Page があるか」で出し分ける。

- 未選択: Sidebar を全画面表示（MainArea 側は非表示。[Empty] の「ページを選択してください」は `md` 以上でのみ出る）
- 選択中: MainArea を全画面表示（Sidebar は非表示）。上部に「← 一覧へ」を置き、click で選択を解除して未選択（＝ Sidebar 全画面）に戻る
- `md` 以上の見た目・寸法はデザイン仕様どおりで変更しない。狭い画面では内側の余白（30px/40px）だけを 16px に詰める

## 状態一覧

| 状態 | 参照画像 | 説明 |
|---|---|---|
| Default | `01_default.png` | 通常表示。Sidebar は選択状態表示のみ、MainArea はタイトル・本文とも表示のみ（各々に Edit ボタン） |
| List Edit Mode | `02_menu_edit.png` | Sidebar 下部の「Edit」ボタンで開始。各行に削除アイコン、下部ボタンが「New page」「Done」に変わる |
| Title Edit | `03_title_edit.png` | MainArea のタイトル横「Edit」で開始。タイトルが入力欄になり、そのボタンが「Cancel」「Save」に変わる |
| Body Edit | `04_text_edit.png` | MainArea の本文横「Edit」で開始。本文がテキストエリアになり、そのボタンが「Cancel」「Save」に変わる |
| Empty | （モックアップ無し） | ページ未選択のとき。MainArea に「ページを選択してください」を出す。**初期表示**（ページを自動選択しない決定）と、**選択中のページを削除した直後**に到達する。モックアップには無いが、到達可能な状態なので用意した（`src/App.tsx`） |

Title Edit と Body Edit は完全に独立していて、**両方を同時に編集中にできる**。片方を編集中でも、もう片方は通常の Edit ボタンのまま残る（`03_title_edit.png` ではタイトルが入力欄・本文側は Edit ボタン、`04_text_edit.png` ではその逆になっているのが確認できる）。

## 状態遷移

### Sidebar

```
[Default]
  --(下部「Edit」ボタン click)--> [List Edit Mode]

[List Edit Mode]
  --(各行の削除アイコン click)--> 確認ダイアログ表示 → OK なら DELETE /content/:id 実行 → リストから除去してトースト「ページを削除しました」（[List Edit Mode] のまま）
                                  ※消したのが選択中のページだった場合、MainArea は [Empty] になる
  --(「New page」click)--> POST /content で空ページ作成 → リストに追加して選択（[List Edit Mode] のまま）
                           → トースト「新しいページを作成しました」＋「取り消す」→ click で DELETE /content/:id
  --(「Done」click)--> [Default]
```

- 要件文書の「メニューの下の『＋』ボタン」＝設計上の「New page」ボタン、要件文書の「『－』ボタン」＝設計上の削除アイコン。同一機能として扱う（`CONTEXT.md` 既定）。
- 削除は実行前に確認ダイアログを出す（モックアップには存在しないが、破壊的操作を防ぐため採用する決定）。実装は `window.confirm`、文言は `「{タイトル}」を削除しますか？`（タイトルが空の場合は `無題` を表示）。
- New page は確認ダイアログを出さず、作成後のトーストで取り消せるようにする（決定事項）。削除は後端に復元 API が無く、再作成すると id と `createdAt` が変わるため、取り消し方式にはしない。
- API エラーは一覧取得・更新・作成・削除とも sonner のトーストで通知する（`src/lib/queryClient.ts` で一括）。文言は操作によらず「処理に失敗しました。時間をおいて再度お試しください」で統一し、HTTP メソッドやステータスコードは出さない（決定事項。利用者には意味がないため）。成功後は `GET /content` を再取得する。

### Title / Body Edit（同じパターンを Title と Body それぞれに適用）

```
[表示中]
  --(その項目の「Edit」click)--> [編集中]（入力欄になる。Edit ボタン → Cancel/Save ボタンに置き換わる）

[編集中]
  --(「Cancel」click)--> 変更を破棄して [表示中] に戻る（API 呼び出しなし）
  --(「Save」click)--> PUT /content/:id を実行（title または body のみを含む部分更新）→ 成功したら [表示中] に戻る
                  失敗したら入力を残したまま [編集中] に留まる（エラーはトーストで通知）
```

- Title の Save は `{ title }`、Body の Save は `{ body }` のみを送る（DTO 上どちらも optional なので個別更新できる。`content.dto.ts` 参照）。
- 選択中ページのタイトルを変更して保存した場合、Sidebar の該当行のラベルも更新する。

## バリデーション

- タイトル: 1〜50文字
- 本文: 10〜2000文字
- 文字数は **`trim()` してから数える**。空白だけの入力は 0 文字扱いになり、下限を満たさないので保存できない（後端の DTO は長さを検証しないため、ここで弾かないと空白だけのタイトルが保存できてしまう）
- **保存する値も `trim()` 後の値**にする。判定に使った値と保存する値が違うと、前後に空白を付けるだけで上限を超えた値（51文字以上のタイトル等）が保存できてしまう。判定と保存はどちらも `src/features/page-editor/useEditSection.ts` の中で行い、セクションごとに書き分けない
- モックアップにエラー表示状態は存在しないため、UI フィードバックは以下の方針で実装する（決定事項）:
  - 文字数が範囲外の間は Save ボタンを非活性にする
  - クリック時のエラー表示（トースト等）は不要。非活性化のみで防ぐ
  - 文字数カウンタは本文のみ入力欄の下に出している（`59/2000文字 (10〜2000文字で入力してください)`）。タイトル側はカウンタなしで Save 非活性のみ（入力欄が1行で、デザイン上カウンタを置く余白がないため）

## API マッピング

この表のとおり `src/api/pageApi.ts` に実装し、`src/features/pages/usePages.ts` から TanStack Query 経由で呼んでいる。

| 操作 | エンドポイント | 備考 |
|---|---|---|
| Sidebar 一覧取得 | `GET /content` | フロントで `createdAt` 降順ソート |
| ページ選択・詳細表示 | 追加リクエストなし | `GET /content` で取得済みの一覧データ（title/body 含む）をそのまま MainArea に表示する（決定事項） |
| New page | `POST /content` | body `{}` で送信 → title/body が `null` のページが作成される（API 層で空文字にそろえる）。body を省くと後端が 500 を返す |
| 削除 | `DELETE /content/:id` | 成功時 204 |
| タイトル保存 | `PUT /content/:id` body: `{ title }` | |
| 本文保存 | `PUT /content/:id` body: `{ body }` | |

## Untitled Page（無題）との整合性

`CONTEXT.md` の定義: `title` が空文字のページは Sidebar 上「無題」とグレー/斜体で表示される。一方バリデーション要件はタイトル1文字以上。矛盾するように見えるが、実際に空タイトルが発生しうるのは **New page 作成直後、まだ一度も保存されていない状態**（バックエンドの `CreateContentDTO.title` は optional）。ユーザーがタイトルを入力して Save するまでは「無題」表示のままで問題ない、という解釈で実装する。空タイトルのまま Save しようとした場合は上記バリデーション方針（Save 非活性）でそもそも実行できない。

## 未確定事項（設計稿に無く、実装時に決めるもの）

- なし（レスポンシブ対応は上記「レスポンシブ（狭い画面）」のとおり実装済み）
